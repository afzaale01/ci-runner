name: 🛡️ Validate Required Repository Inputs

on:
  workflow_call:
    inputs:
      requiredVariables:
        description: 'JSON array of required GitHub variables to validate'
        required: true
        type: string
    secrets:
      CICD_PAT: { required: true }
      UNITY_EMAIL: { required: true }
      UNITY_PASSWORD: { required: true }
      UNITY_LICENSE: { required: true }

jobs:
  validate_github_variables:
    name: Validate Required GitHub Variables
    runs-on: ubuntu-latest
    steps:
      - name: Validate Required GitHub Variables
        run: |
          REQUIRED_VARIABLES=$(echo '${{ inputs.requiredVariables }}' | jq -r '.[]')
          ALL_VARIABLES_JSON='${{ toJson(vars) }}'
          MISSING_VARIABLES=()
      
          for VARIABLE in $REQUIRED_VARIABLES; do
            if ! echo "$ALL_VARIABLES_JSON" | jq -e --arg key "$VARIABLE" 'has($key)' > /dev/null; then
              MISSING_VARIABLES+=("$VARIABLE")
            fi
          done
      
          if [[ ${#MISSING_VARIABLES[@]} -gt 0 ]]; then
            echo "❌ Missing required GitHub Variables:"
            for VARIABLE in "${MISSING_VARIABLES[@]}"; do
              echo "  - $VARIABLE"
            done
            exit 1
          fi
      
          echo "✅ All required GitHub Variables are defined."

  validate_github_secrets:
    name: Validate Required GitHub Secrets
    runs-on: ubuntu-latest
    steps:
      - name: Validate Required GitHub Secrets
        run: |
          REQUIRED_SECRETS=("CICD_PAT" "UNITY_EMAIL" "UNITY_LICENSE" "UNITY_PASSWORD")
          MISSING=0

          for SECRET_NAME in "${REQUIRED_SECRETS[@]}"; do
            VALUE_VAR="SECRET_${SECRET_NAME}"
            VALUE="${!VALUE_VAR}"

            if [[ -z "$VALUE" || "$VALUE" == "" ]]; then
              echo "❌ Required secret '$SECRET_NAME' is not set!"
              MISSING=1
            else
              echo "✅ Secret '$SECRET_NAME' is present."
            fi
          done

          if [[ "$MISSING" -eq 1 ]]; then
            echo "❌ One or more required secrets are missing. Failing..."
            exit 1
          fi
        env:
          SECRET_CICD_PAT: ${{ secrets.CICD_PAT }}
          SECRET_UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          SECRET_UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          SECRET_UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}