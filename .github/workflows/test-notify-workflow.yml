name: 🧪 Test Notify Workflow

on:
  workflow_dispatch:

jobs:
  test-notify:
    uses: avalin/unity-ci-templates/.github/workflows/step-5-notify.yml@main
    with:
      releaseResult: "false"
      deployResult: "false"
      version: "v1.2.1"
    secrets: inherit
      
